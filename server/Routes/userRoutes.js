 
 
 const express= require('express');
 const router = express.Router();
 const User=require ('../Models/userModel')
 const middleware=require ('../Middleware/authMiddleware')
 const {register,login,LoadUserinfo,getUsers}=require('../Controllers/userController');

 const { validationCheck } = require('../middleware/dataCheckMiddlewar');


/////////////////////////////////////////////////////////////////////////////
router.post('/register',validationCheck,register)
/////////////////////////////////////////////////////////////////////////////
router.post('/login',login)
/////////////////////////////////////////////////////////////////////////////
router.get('/deleteAll',async (res) => {await User.deleteMany({}); res.send('done')})
///////////////////////////////////////////////////////////////////////////
router.get('/loaduser',middleware,LoadUserinfo)
/////////////////////////////////////////////////////////////////////////////
router.get('/',getUsers)
/////////////////////////////////////////////////////////////////////////////

module.exports=router;